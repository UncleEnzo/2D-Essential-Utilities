namespace Nevelson.Utils
{
    public static class Enums
    {
        public enum OnSceneLoad { PERSIST, RESET }
        public enum Direction { LEFT, RIGHT, UP, DOWN, UP_LEFT, UP_RIGHT, DOWN_LEFT, DOWN_RIGHT, NONE }
    }
}